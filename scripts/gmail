#! /usr/bin/sh
user="user"
password="password"

correos=`curl -m 5 https://$user:$password@mail.google.com/mail/feed/atom | grep -o '<fullcount>.*</fullcount>' | grep -o '[0-9]*'`
if [[ $correos == "" ]]; then
    correos=-1
fi
echo $correos > $HOME/._inbox_
