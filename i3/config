# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
#font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1

# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
# font pango:DejaVu Sans Mono 10
font pango:Droid Sans 10px

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec urxvt -e bash -c "tmux -q has-session && exec tmux attach-session -d || exec tmux new-session -s$USER" #i3-sensible-terminal
bindsym Mod1+Control+t exec urxvt

# kill focused window
bindsym $mod+q kill
#bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
#bindsym $mod+d exec synapse
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# ASIGANAR LOS ESPACIOS DE TRABAJO A VARIABLES
#=============================================
set $WS1 1
set $WS2 2
set $WS3 3
set $WS4 4
set $WS5 5
set $WS6 6
set $WS7 7
set $WS8 8
set $WS9 9
#--------------------------------------------

# WINDOW CONTROL
## Float everything
for_window [window_role="pop-up"] floating enable,move absolute center
for_window [class="mplayer2"] floating enable
# for_window [title="Skype"] floating enable
for_window [class="Arandr"] floating enable
#for_window [title="download"] floating enable
for_window [title="TrueCrypt"] floating enable
#for_window [title="Browser"] floating enable
# for_window [title="Catfish"] floating enable
for_window [title="Progreso de operación de archivo" class="Thunar"] floating enable
for_window [title="Operaciones con archivos" class="Marlin"] floating enable
for_window [title="Comparte un enlace en Twitter - Mozilla Firefox" class="Firefox"] floating enable
for_window [title="Press This - Mozilla Firefox" class="Firefox"] floating enable
for_window [title="Preferences"] floating enable
for_window [class="Gsimplecal"] floating enable
for_window [class="Alarm-clock"] floating enable
for_window [class="Viewnior"] floating enable
for_window [class="Lxappearance"] floating enable
for_window [class="Nitrogen"] floating enable
for_window [title="Xfce-notifyd"] floating enable
for_window [class="mousepad"] floating enable
for_window [class="Pidgin"] floating enable
for_window [title="Lista de amigos"] floating disable
for_window [class="Wicd-client.py"] floating enable
for_window [class="Oblogout"] floating enable fullscreen global
for_window [class="I3exit"] floating enable fullscreen global
for_window [class="Lxtask"] floating enable
for_window [class="Tilda"] floating enable
for_window [title="tilda"] floating disable
for_window [class="Gpicview"] floating enable
for_window [class="Synapse"] border none floating enable
for_window [class="XCalc"] floating enable
for_window [title="Eclipse |Eclipse" class="Java"] floating enable
#for_window [class="[.]*"] floating enable
#assign [class="mplayer2"] $WS4
assign [class="VirtualBox"] $WS6
assign [class="libreoffice*|Evince"] $WS7
# assign [class="Sylpheed"] $WS8
# assign [instance="VCLSalFrame"] $WS7
## BROWSER ASSIGNMENT (Navigator=Iceape)
assign [instance="Navigator|chromium|filezilla|transmission-gtk|google-chrome"] $WS2
#for_window [class="URxvt"] floating enable
#for_window [class="URxvt"] border 1pixel
assign [class="Gimp|Eclipse|Pinta|Blender|Catfish|Galculator"] $WS5
assign [title="Eclipse |Eclipse|SDK de Eclipse " class="Java"] $WS5
#for_window [class="Thunar"] floating enable
assign [instance="mousepad|leafpad|geany"] $WS3
#assign [instance="terminator|urxvt"] $WS1
assign [instance="shotwell|nitrogen|simple-scan|quodlibet|gnome-mplayer|brasero|devede|imagination|picard|wxcam|google-musicmanager|sound-juicer"] $WS4
assign [instance="syncbackup|bleachbit|caffeine|gparted|hardinfo|truecrypt"] $WS6
#for_window [class="Mousepad"] floating enable #assign [class="Turpial" ] $WS4
#for_window [class="Turpial" ] floating enable
assign [class="Geary|Liferea|Pidgin|Skype"] $WS8
assign [class="JDownloader"] $WS9

bindsym Mod1+Tab focus right




# change focus
# j - k - l - ntilde
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
#bindsym $mod+Left focus left
#bindsym $mod+Down focus down
#bindsym $mod+Up focus up
#bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+H move left
bindsym $mod+Shift+J move down
bindsym $mod+Shift+K move up
bindsym $mod+Shift+L move right

# alternatively, you can use the cursor keys:
# bindsym $mod+Shift+Left move left
# bindsym $mod+Shift+Down move down
# bindsym $mod+Shift+Up move up
# bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+Right split horizontal

# split in vertical orientation
bindsym $mod+Down split vertical

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen
#bindsym $mod+Shift+f fullscreen global

# change container layout (stacked, tabbed, toggle split)
# bindsym $mod+y layout stacking
# bindsym $mod+u layout tabbed
#bindsym $mod+d layout toggle split
bindsym $mod+i layout toggle 

# toggle tiling / floating
bindsym $mod+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+Tab focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+d focus child

# CAMBIAR ESTILO DE BORDE
bindsym $mod+t border normal
bindsym $mod+Shift+t border pixel 0 
#bindsym $mod+y border 1pixel 
#bindsym $mod+u border none 

# Back and Forth
workspace_auto_back_and_forth yes


# switch to workspace
bindsym $mod+1 workspace $WS1
bindsym $mod+2 workspace $WS2
bindsym $mod+3 workspace $WS3
bindsym $mod+4 workspace $WS4
bindsym $mod+5 workspace $WS5
bindsym $mod+6 workspace $WS6
bindsym $mod+7 workspace $WS7
bindsym $mod+8 workspace $WS8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $WS1
bindsym $mod+Shift+2 move container to workspace $WS2
bindsym $mod+Shift+3 move container to workspace $WS3
bindsym $mod+Shift+4 move container to workspace $WS4
bindsym $mod+Shift+5 move container to workspace $WS5
bindsym $mod+Shift+6 move container to workspace $WS6
bindsym $mod+Shift+7 move container to workspace $WS7
bindsym $mod+Shift+8 move container to workspace $WS8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10

# reload the configuration file
# bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
# bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
# bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"
# las opciones anteriores en un nagbar
# bindsym $mod+Shift+e exec "i3-nagbar -t warning -m '' -b 'Salir de i3' 'i3-msg exit' -b 'Reiniciar i3' 'i3-msg restart' -b 'Recargar i3' 'i3-msg reload'"
# ahora con mode
set $optconf Preciona la letra [L]reload i3    [R]reiniciar i3
mode "$optconf" {
    bindsym l reload mode "default"
    bindsym r restart mode "default"
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+Shift+r mode "$optconf"


# resize window (you can also use the mouse for that)
set $resize Cambiar tamaño
mode "$resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        # bindsym Left resize shrink width 10 px or 10 ppt
        # bindsym Down resize grow height 10 px or 10 ppt
        # bindsym Up resize shrink height 10 px or 10 ppt
        # bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "$resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
#        status_command i3status
        i3bar_command i3bar-icons
        status_command $HOME/.aek6/i3/conky.sh
#        tray_output none
        position top
        mode dock
#       font pango:fixed 6
        font pango:Convergence 10px
  colors {
        statusline      #8e8100
        background      #222222
       
# class                         border          backgrd         text
        focused_workspace  #222222              #222222         #00ddff
        active_workspace   #222222              #222222         #696f89
        inactive_workspace #222222              #222222         #777777
        urgent_workspace   #000000              #000000         #afd700
   }
}

#bar{
#    mode hide
#    hidden_state hide
#    modifier Mod1
#    colors{
#        background  #444444
#    }
#    font pango:Convergence 20px
#    workspace_buttons no
#    tray_output none
#}
#bindsym $mod+m bar hidden_state toggle
#bindsym $mod+n bar hidden_state hide 
#bindsym $mod+b bar mode toggle

#-----------------------------

# INICIANDO DAEMONS Y APLICAIONES
#==============================
# iniciando daemons
exec --no-startup-id ~/.aek6/i3/autostart

# control de energia con dpms y el lock
# exec --no-startup-id xset dpms 300 0 0
# exec --no-startup-id xset s 180 0
# exec --no-startup-id xss-lock -- $HOME/.aek6/i3/i3-lock.sh &

# AGREGANDO COMANDOS
#==============================
# Borde estilo
new_float       pixel 0
new_window      pixel 0

# hide_edge_borders both

#Colores
#> lineas_de_titulo  borde  texto  borde_salida
#client.focused #ff0000 #ff5a00 #ffffff #ffff00
client.focused                  #ffa800 #ffa800 #111111 #900000
#client.unfocused                #444444 #444444  #315858
#client.focused_inactive         #444444 #444444  #2c2c2f
client.urgent                   #383a3b #383a3b  #ee0000
client.focused_inactive         #787878 #787878 #000000
client.unfocused                #787878 #787878 #ffffff 
#client.urgent #ff0000 #ff0000 #ffffff #ffff00
#client.background #ff00ff

# mouse
focus_follows_mouse no 

# Focus wrapping
# force_focus_wrapping yes

# popup en modo pantalla completa
popup_during_fullscreen smart

# retrasar urgencia
force_display_urgency_hint 500 ms

# scratchpad
bindsym $mod+minus scratchpad show
bindsym $mod+Shift+minus move scratchpad


#-----------------------------

#COMANDOS PERSONALIZADOS
#==============================
#Aplicaciones
bindsym $mod+e exec --no-startup-id  thunar
bindsym Mod1+Control+Delete exec --no-startup-id lxtask
bindsym $mod+c exec --no-startup-id xcalc
bindsym $mod+Control+c exec --no-startup-id zenity --calendar

# Acciones
# control de mpd por sonata
# bindsym $mod+Control+Right exec sonata next
# bindsym $mod+Control+Left exec sonata prev
# bindsym $mod+p exec sonata play
# bindsym $mod+Shift+p exec sonata pause
# Control de mpd con ncmpcpp
bindsym $mod+Control+Right exec ncmpcpp next
bindsym $mod+Control+Left exec ncmpcpp prev
bindsym $mod+p exec ncmpcpp toggle
bindsym $mod+KP_Add exec ncmpcpp volume +10
bindsym $mod+KP_Subtract exec ncmpcpp volume -10
# captura de pantalla usando imlib2_grab e import
bindsym Print exec --no-startup-id $HOME/.aek6/scripts/captura_imlib2.sh
bindsym --release $mod+Print exec --no-startup-id $HOME/.aek6/scripts/captura_import.sh
# Parar/iniciar xset, ahorro de energia (dpms screensaver)
bindsym Mod1+Control+e exec xset -dpms s off
bindsym Mod1+Control+Shift+e exec xset +dpms s on
# Control de volumen
bindsym $mod+Control+Up exec --no-startup-id $HOME/.aek6/scripts/volumen up
bindsym $mod+Control+Down exec --no-startup-id $HOME/.aek6/scripts/volumen down
bindsym $mod+Control+m exec --no-startup-id $HOME/.aek6/scripts/volumen mute
#-----------------------------


# salir 
bindsym $mod+Escape exec python2 $HOME/.aek6/i3/i3exit

#bindsym Mod1+Control+BackSpace exec "i3-nagbar -t warning -m '' -b 'Apagar' 'shutdown -h now' -b 'Reiniciar' 'reboot' -b 'Suspender' '' -b 'Cerrar sesion' 'i3-msg exit' -b 'Bloquear' 'i3lock -n '"
